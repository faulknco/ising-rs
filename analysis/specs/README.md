# Specs

This directory holds schemas and examples for reproducible research metadata.

- `run-manifest.schema.json` defines the minimum metadata expected for simulation and analysis runs.
- `example-run-manifest.json` provides a concrete example payload that future scripts should be able to emit.
