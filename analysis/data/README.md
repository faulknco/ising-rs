# Analysis Data

This directory contains generated research data products and their support files.

- `raw/` holds immutable simulation outputs.
- `derived/` holds scripted analysis outputs.
- `manifests/` holds provenance metadata for runs and derivations.

Historical top-level CSV files still exist during the migration, but new work should use the structured subdirectories.
