# Raw Data

Place immutable simulation outputs here.

Examples:

- finite-size sweep CSVs
- raw Wolff time-series outputs
- disorder-realization outputs
- Kibble-Zurek sweep outputs

Raw files should be accompanied by a manifest in `analysis/data/manifests/`.
